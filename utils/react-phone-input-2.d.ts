declare module "react-phone-input-2" {
  export interface PhoneInputProps {
    /**
     * Shows the dial code (`+94`) next to the flag instead of inside the input.
     */
    separateDialCode?: boolean;
  }
}
